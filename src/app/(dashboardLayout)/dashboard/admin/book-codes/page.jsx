// One code per printed copy. Minting a run, and the record of who opened which.
import BookCodesManager from '@/components/admin/bookCopy/BookCodesManager';

export const metadata = { title: 'Book codes' };

export default function BookCodesPage() {
  return <BookCodesManager />;
}
